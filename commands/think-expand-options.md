---
description: Break past the obvious option set by reframing the problem, transforming the seed with SCAMPER, and reversing its foundational assumptions, then shortlisting candidates.
argument-hint: "[the decision, problem, or material to work through]"
maps-to: think-expand-options
---
<!-- GENERATED by scripts/gen-recipe-commands.mjs from _workflows/think-expand-options.md - do not edit. Regenerate: npm run gen:recipe-commands -->

Run the **think-expand-options** recipe: 3 skills chained in a fixed order, carrying forward only the compressed artifact at each handoff.

The compression is the point. Do not paste a whole artifact into the next step; carry exactly what each handoff names, so the chain stays cheaper and sharper than running the skills back to back.

Run the steps in order, carrying forward only the compressed artifact between them:

1. `think-problem-restatement` -> carry the **chosen working frame**. (For blank-page breadth, swap in `think-brainwriting`.)
2. `think-scamper` -> carry the **SCAMPER shortlist**.
3. `think-assumption-reversal` -> carry the **combined shortlist** of candidates (flagged as candidates, not decisions).

Composite artifact: a de-duplicated shortlist of options spanning incremental variations and assumption-breaking alternatives, ready to hand to `think-decision-option-review`. Full prose: `recipes/expand-options.md`.

Apply the chain to: $ARGUMENTS
