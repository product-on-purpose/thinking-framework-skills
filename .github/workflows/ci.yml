name: Conformance gate

# G2 (self-hosting CI): runs the agent-skills-toolkit conformance gate against this
# plugin on every PR and push to main. Per Standard sec 4.4, this workflow contains
# NO validation logic of its own; it only invokes the portable gate `node scripts/check.mjs`,
# which a contributor reproduces locally by cloning the toolkit next to this repo.
#
# The validators live in the public agent-skills-toolkit (the source of truth for the
# checks). We pin a ref for reproducibility; bump it deliberately to adopt toolkit changes.
#
# WHAT THIS WORKFLOW DOES NOT RUN, and why: the BEHAVIORAL evals (trigger routing and output
# quality) are deliberately offline. They execute real skills against a live model, so they cost
# real tokens, are non-deterministic run to run, and produce the numbers published on the trust
# page - none of which belongs on a per-PR gate. They are run by hand on behaviour-affecting
# releases and their scorecards are committed under docs/internal/eval-results/, where the
# eval-results layer of `check.mjs` DOES gate their shape and pairing. So CI enforces that a
# published measurement is well-formed; a human decides when to re-measure.
# See scripts/eval/README.md for the operator flow.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v5

      - name: Checkout the toolkit validators (pinned)
        uses: actions/checkout@v5
        with:
          repository: product-on-purpose/agent-skills-toolkit
          ref: 93da438812c2eeedf51daf790379246d6d6acff0
          path: .agent-skills-toolkit

      - name: Setup Node (pinned via .nvmrc)
        uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          # Cache the toolkit's deps, not this repo's: the npm ci below runs in
          # .agent-skills-toolkit, so that is the lockfile the cache key must follow.
          cache: 'npm'
          cache-dependency-path: .agent-skills-toolkit/package-lock.json

      - name: Install toolkit dependencies
        run: npm ci
        working-directory: .agent-skills-toolkit

      - name: Run conformance gate
        env:
          AGENT_SKILLS_TOOLKIT: ${{ github.workspace }}/.agent-skills-toolkit
        run: node scripts/check.mjs

  # Drift guard for the ONE committed generated artifact: the advisor's name-safety
  # set (skills/think-framework-advisor/references/recommendable.{json,md}), produced
  # by scripts/gen-recommendable.mjs from library.json + SKILL.md frontmatter +
  # _workflows/*.md. The generator is zero-dependency, so this needs no install. If a
  # skill or recipe is added/renamed/removed without regenerating, --check exits 1 and
  # the build fails here instead of shipping an advisor that names a nonexistent skill.
  recommendable-drift:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v5

      - name: Setup Node (pinned via .nvmrc)
        uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'

      - name: Check advisor recommendable set is in sync
        run: node scripts/gen-recommendable.mjs --check

  # PR-time, NON-deploying site build: runs the generator (gen-site.mjs) + astro build
  # so generator/build breakage is caught before merge. Deployment stays solely in
  # deploy-pages.yml (push to main / workflow_dispatch); this job uploads nothing.
  site-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: site
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v5

      - name: Setup Node (pinned via .nvmrc)
        uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          # This job's defaults.run.working-directory is site/, so the npm ci below installs
          # site/package-lock.json. The cache key must name that lockfile explicitly; the
          # action does not inherit the job's working-directory when resolving it.
          cache: 'npm'
          cache-dependency-path: site/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build site (gen-site.mjs + astro build)
        id: build
        run: npm run build

      # Link/route integrity guards (clause 14.11), run against the freshly built dist from the
      # site working dir. STRICT_ANCHORS=1 enforces #anchors on this partly hand-authored site.
      # Gated on the build step's own outcome (not `if: always()`): a build failure then surfaces
      # the real Astro error instead of a secondary "dist not found", and both guards still report
      # independently of each other (a rendered-link failure does not skip the route-parity check).
      - name: Check rendered links in dist (clause 14.11a)
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        run: node ../scripts/check-rendered-links.mjs dist
        env:
          STRICT_ANCHORS: '1'

      - name: Check route parity against the committed manifest (clause 14.11b)
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        run: node ../scripts/check-route-parity.mjs dist

      - name: Check mermaid diagrams in generated site content
        if: ${{ !cancelled() && steps.build.outcome == 'success' }}
        run: node ../scripts/check-mermaid.mjs src/content/docs

  # Robustness proof for the rendered-link guard (clause 14.11, "guard robustness is itself
  # normative"): a node --test suite over synthetic dist fixtures (clean, broken link, empty dist,
  # bare-relative, both quote styles, defensive decode). Node built-ins only, so no install or build.
  guard-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v5

      - name: Setup Node (pinned via .nvmrc)
        uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'

      - name: Run link-guard unit tests
        run: npm test
