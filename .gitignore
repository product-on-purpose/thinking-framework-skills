# Local working material and scratch (raw research, transcripts, throwaway notes)
_local/

# Agent session context and logs (local for now)
_agent-context/

# memsearch plugin local state
.memsearch/

# Playwright MCP scratch (screenshots, snapshots, console logs from local testing)
.playwright-mcp/

# The conformance gate clones the toolkit validators here (CI) or expects a sibling.
.agent-skills-toolkit/
node_modules/

# Claude Code local agent state (worktrees, local settings) - never committed.
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
