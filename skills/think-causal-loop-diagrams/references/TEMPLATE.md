# Signed Causal Loop Diagram - Template

Fill this in. The deliverable is the signed loop inventory plus the behavior read-out, not prose. Every loop must be genuinely closed (the chain returns to a variable it started from) and every link must carry a polarity (+ same direction, - opposite direction). Mark open chains as linear - do not force them closed. The read-out is an argument, not a prediction.

---

## Variables in play

- [variable A], [variable B], [variable C], ... (each should be able to rise or fall, not an event)

## Closed loops (signed)

Each loop returns to a variable it started from. Write the link path with a polarity (+/-) on every arrow, then label the loop by the product of its signs: even number of negatives = **R** (reinforcing), odd number = **B** (balancing). Number them R1, B1, ...

| Loop | Link path with polarities | Net sign | Label (R/B) | Delay? | What this loop does |
|---|---|---|---|---|---|
| R1 | A -(+)-> B -(+)-> A | + | R (reinforcing) | no | amplifies: spiral |
| B1 | C -(+)-> D -(-)-> C | - | B (balancing) | yes | counteracts: goal-seeking, oscillates if delayed |

## Open / linear parts (no loop closed - recorded honestly)

- [chain that does not feed back]: linear, set aside. (Do NOT manufacture a loop here.)

## Behavior read-out (an argument, not a prediction)

- **Dominant loop right now:** [R# or B#], because [why it currently outweighs the others].
- **Resulting dynamic:** [spiral (vicious / virtuous) / goal-seeking / oscillation], i.e. [the behavior the structure argues for].
- **What would flip dominance:** [the condition under which a different loop takes over and the dynamic changes].
- **Honest scope:** this is a structured argument about likely dynamics from the loop structure, not a forecast of values or dates; different modelers may sign the loops differently (CLD reliability is a known limit).
