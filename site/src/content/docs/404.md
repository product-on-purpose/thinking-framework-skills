---
title: '404'
template: splash
editUrl: false
hero:
  title: Page not found
  tagline: That page is not here. Try the search bar, or start from one of these.
  # Links hardcoded below (clause 14.7 sanctioned exception, alongside the test value-pin
  # and the robots.txt sitemap URL): a 404 is served from any path, so its links must be
  # root-absolute to resolve, and YAML frontmatter cannot read import.meta.env.BASE_URL.
  # The site is served at the custom-domain root, so the base is empty and these are
  # plain root-absolute paths (no project subpath).
  actions:
    - text: Start here
      link: /start/getting-started/
      icon: right-arrow
      variant: primary
    - text: Browse the frameworks
      link: /frameworks/
      icon: open-book
    - text: Find one by your situation
      link: /explore/chooser/
      icon: puzzle
---
