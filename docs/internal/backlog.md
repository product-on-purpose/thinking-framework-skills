# Backlog - deferred items

Durable record of work consciously deferred, so it survives past local scratch (the SDD ledgers in `.superpowers/sdd/` and the codex review notes in `_agent-context/` are gitignored). Each actionable item also has a GitHub issue. Update this file when an item lands or a decision changes.

## From the v0.12.0 docs effort (2026-06-20)

The four-workstream docs platform (site changelog, CI guards + gate 9 -> 13, docs audit, mermaid diagrams) shipped as v0.12.0. These were deferred.

### CI-guard hardening (the guards all work; these extend coverage/robustness)

- **[#91] Extend the canonical-link guard to `.astro` + `site/intros/`.** It scans `.md`/`.mdx` under `content/docs` only; the two real redirect-hops in `Chooser.astro` and an intro source were hand-fixed, but the guard will not catch a future hop in those file types. *Addressed in the guard-hardening PR (closes #91).*
- **[#93] Extract a shared `scripts/lib/walk.mjs`.** Four guards have near-duplicate directory walkers. *Addressed in the guard-hardening PR (closes #93).*
- **[#92] Harden `mermaid-lint`.** Add `[` to the token-split set; add tests for the directive/comment-skip + `.mdx` scope; handle 4+ backtick inline fences. (A real headless `mermaid.parse()` stays optional - the Mermaid MCP covers semantics at authoring time.) *Addressed in the guard-hardening PR (closes #92).*
- **[#94] `servedPath` empty-slug guard.** An empty-string frontmatter `slug:` would yield `//`. Degenerate, not reachable today. *Addressed in the guard-hardening PR (closes #94).*

### Trust page / evals

- **[#95] Emit a contested-output-eval `.json`.** DONE (2026-06-24, PR #98 / eval-harness orchestration). The `.json` was regenerated THROUGH the scorer (`scripts/eval/reconstruct-contested-output.mjs`), verified by a body byte-match against the committed `.md` (which was left untouched), and the new 14th gate layer (`scripts/check-eval-results.mjs`) now enforces that every scorecard stays a paired `.md` + `.json`. Closed.
- **[#96] One-pass eval across all 63 skills.** The trust page reports the core-56 run (2026-06-17) and the contested cohort (2026-06-19) as separate cohorts; a single fresh run would unify them. Deliberately out of scope (no skill changes). **Still open**, but as of PR #98 it is now a cheap 3-command run (extract -> route + output Workflows -> `finalize.mjs`); the only reason it stays open is that it is a behavioral re-run with nothing behavioral to re-measure. Run it on the next behavior-affecting release.

### Deliberate decisions (no action expected)

- **`INDEX.md` "Skills (67)".** Generated by the external `agent-skills-toolkit` (`gen-index`); "67" = 63 frameworks + 4 tools (internally consistent, just unexplained). Fixing the label needs a toolkit change, not a change here. No issue (upstream).
- **Four authoring commitments - consistency, not centralization.** v0.12.0 made the four commitments consistent across all six surfaces with a canonical pointer to `about/philosophy.md`; full centralization (one source, others link only) was deliberately not done because it harms each surface's readability. Decided; not an open item.

## From the eval-harness orchestration effort (2026-06-24)

Shipped as PR #98 (`f12f532`, rides `[Unreleased]`): `finalize.mjs` (one step writes both the `.md` and `.json` scorecard into `docs/internal/eval-results/` and stamps `skill.meta.yml`), the pure `score-lib.mjs` extraction, the 14th gate layer (eval-results pairing + shape), and the #95 retro-fix. These were deferred.

- **Task 8 - combined-run Workflow (`scripts/eval/eval.workflow.mjs`).** Would run routing + produce/judge in one Workflow call so the operator flow is 3 commands instead of 4. Pure convenience; the provenance fix does not depend on it, and its smoke-test is a live model-executed run. No issue (low priority).
- **Minors (fine-to-defer, from the per-task + whole-branch reviews).** No positive/negative TRIGGER-eval shape unit test in `tests/check-eval-results.test.mjs` (the OUTPUT path is unit-tested; the trigger path is covered only by the real-tree runner, though both share the same `REQUIRED_TOTALS` loop); `finalize.mjs` `main` has no try/catch (a failure still exits non-zero, just less cleanly); `tests/finalize.test.mjs` does not assert the JSON trailing newline. All triaged fine-to-defer by the final review; none block. (The one Minor worth fixing now, an unused import, was cleaned up in the branch.)

## From the lifecycle-metadata truth effort (2026-08-14)

Shipped on `fix/lifecycle-metadata-truth` (`2057be1`): all 67 sidecars promoted to `status: active`, 63 to `maturity: measured`, a new lifecycle-truth section in `check-registry.mjs` backed by a unit-tested `scripts/lib/lifecycle-lib.mjs`, the stale 34-skill corpus count removed from both `engine.md` copies, and the conformance claim scoped to Standard 0.8. These were deferred or decided.

### Deferred

- **`library.json` homepage + native manifest regeneration (audit finding A-03).** `homepage` still points at the GitHub repo rather than the live site. The fix is one line, but it must ship together with regenerated `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, `manifest.generated.json` and `INDEX.md`, and regenerating those requires the **CI-pinned** toolkit ref. The local sibling checkout is ahead of the pin, so doing it with what was to hand would have produced a diff CI disagrees with. Do it as the first step of the next release cut, where the pinned regeneration already happens (release-process step 3).
- **Research-agent description rewrite (finding B-03).** The paired "Tool constraints (hard limits)" hardening is safe to do any time; the description rewrite changes a routing surface and should be measured, so it waits behind an eval run.

### Decided (no further action expected)

- **CLOSED 2026-09-10: C2-7 / ADV-1 (the unmeasured front door) - the instrument was built and the front door has a number.** The four meta-skills now read `trigger_eval_status: measured-2026-09-10`, scored by the new **skill-selection eval** against the installed roster (67 skills + 10 commands) rather than the advisor's 63-framework corpus. **Trigger top-1 16/17 (94%), no-false-fire 21/21 (100%), right-alternative 12/13, zero false fires**, one gate case excluded by construction. Scorecard: `docs/internal/eval-results/2026-09-10-skill-selection-trigger-eval.{md,json}`. The diagnosis that produced it is kept below, because the reasoning is the reusable part.
  - **Why it stayed open for three months.** `think-framework-advisor`, `think-top3`, `think-random-frameworks` and `think-research-framework` all report `trigger_eval_status: authored`. This is not a missing run; it is a missing instrument.
  - **The mechanism.** `scripts/eval/route.workflow.mjs` has blind agents route each situation against `skills/think-framework-advisor/references/recommendable.json`. That file contains exactly the **63 shipped frameworks**; the four meta-skills are not in it, and should not be - it is the advisor's own corpus of things to recommend, not a roster of installed skills. So the router cannot name a meta-skill as top1 no matter how well the meta-skill is described.
  - **Measured, 2026-09-10.** Of the 39 cases across the four meta-skills' `eval/cases.md`: **22 validly scoreable** (8 anti-cases naming a shipped framework, 14 expecting `none`) and **17 unscoreable by construction** (5 advisor, 4 top3, 4 random-frameworks, 4 research-framework trigger cases, each expecting a slug absent from the catalog). Running the existing harness would score those 17 as failures and publish a fabricated near-zero.
  - **The two questions are different, and only one has an instrument.** Framework routing asks *which of the 63 frameworks fits this situation* - measured, 100% top-1, 0 false-fires. Skill selection asks *which of the installed skills does an agent invoke for this prompt* - unmeasured. The advisor's trigger contract is the second question. So is every anti-trigger in its `cases.md` ("Run a premortem" should reach `think-premortem` **directly**, not via the advisor), and so is the over-grab risk of any new surface.
  - **What was built** (`scripts/eval/skill-selection.workflow.mjs` + `scripts/lib/selection-lib.mjs` + `scripts/eval/extract-roster.mjs` + `scripts/eval/score-selection.mjs`): a sibling workflow routing against the **`name` + `description` pairs an agent actually sees** (the 67 skills plus the 10 commands), rather than against `recommendable.json`. That instrument would (a) give the four meta-skills their first real trigger baseline, closing ADV-1; (b) be the correct instrument for **guardrail 6**, which the current one cannot satisfy - see the note below; and (c) make the advisor-rewrite proposal in the 2026-07-10 audit decidable on evidence, since it argues for a description change and there is currently no way to score one.
- **ANSWERED 2026-09-10: guardrail 6's command-interference question, measured at last.** The nine recipe commands (plus `think-research-framework`, which ships as both a skill and a command) compete directly against all 67 skills in the skill-selection corpus. Across **794 cases, commands took the top pick exactly twice** (0.25%): once on a `dialectical-bootstrapping` anti-case whose expected answer was `none` anyway, and once on the single advisor trigger case that also failed in the pilot run. Commands appeared *somewhere* in top-3 84 times, so they are visible to a router and simply not winning. **The added command surface did not measurably steal skill selections.** That is now evidence, not an assumption - and note it took a new instrument to get it, which is the point of the note below.
- **Note on guardrail 6 as it was written, kept because the reasoning still applies to the next surface.** The guardrail requires a footprint delta *and* a routing re-run for every new command, recipe or subagent. The re-run was done for v0.14.0 and reproduced cleanly, but it **cannot detect what the guardrail is worried about**: the nine recipe commands added always-loaded *description* surface, while the trigger eval routes against `recommendable.json`, which the commands never touched (verified: unchanged since 2026-06-19). So the re-run is a genuine reproducibility check and a genuine confirmation that catalog routing is intact - but it is not evidence that the added surface did not interfere with skill selection, because it does not look there. The skill-selection instrument above is what would make guardrail 6 mean what it says.
- **OPEN, and diagnosed: `think-research-framework` cannot be output-evaluated by the generic produce-then-judge harness.** It is the one meta-skill still at `output_eval_status: authored`, and again the blocker is instrument fit rather than neglect. Three independent reasons, each sufficient on its own:
  - **Its own SKILL.md forbids what the harness asks for.** Step 2 reads: *"Dispatch the work to the `think-research-framework` subagent ... **Do not run the research inline in this context**; the subagent holds the honesty discipline and runs the web research in its own window."* The generic produce prompt instructs an agent to read `SKILL.md` + `references/TEMPLATE.md` and emit the artifact inline. Running it would measure the skill doing the opposite of its documented procedure.
  - **It has no `references/` directory at all**, so there is no `TEMPLATE.md` for the produce prompt to read. It is a dispatcher, not an artifact-emitting method; the artifact structure lives in the subagent at `agents/think-research-framework.md`.
  - **Its output checks require real web research, and an inline producer would fabricate.** The checks demand "3 to 6 real sources (authors, year, what was measured)" and that the run "refuse any widely-quoted statistic with no traceable primary source rather than laundering it into the grade". The subagent declares `tools: Read, Glob, Grep, WebSearch, Write, Bash` and carries the "REAL SOURCES (hard rule)" non-negotiable in its own system prompt. A produce agent following the generic prompt is never told to research anything, inherits none of that discipline, and would be graded on citations it had no instructed means of checking. A judge passing fabricated citations would return a number that **rewards** the single failure this library exists to prevent.
  - **What would close it:** a dispatch-aware produce stage that invokes the real subagent (`Task` with `subagent_type: think-research-framework`) on a bounded, cheap research brief, and judges the dossier + proposed registry entry it returns - including running `scripts/check-proposed-entry.mjs` against that entry as a deterministic check rather than a model judgment. That is a new harness stage plus live web research, so it is scoped and maintainer-gated rather than taken unattended.
  - **Why it was not forced.** The same call as the trigger-eval diagnosis on 2026-09-10: a number produced by the wrong instrument is worse than an honest gap, because it looks like evidence. `think-research-framework` keeps `maturity: alpha` with the reason recorded in its sidecar.
- **OPEN, found by the eval it was meant to grade: the advisor's "what NOT to use" check does not test whether a decline is CORRECT.** In the 2026-09-10 output run the advisor declined the `think-stress-test-decision` recipe on the grounds that it "presupposes an option comparison you have not done". That recipe's step 1 **is** `think-decision-option-review` - the option comparison - so the decline rests on a false premise. Two independent judges flagged it (the `framework-advisor` judge and, separately, the `top3` judge), and it was then verified deterministically against `recommendable.json`. **The check passed anyway**, because as written it requires only a non-empty section that includes the calibrator's cuts. A wrong decline is invisible to it.
  - Deliberately **not** fixed in the same PR that measured it: editing a check mid-measurement is how a number stops meaning anything. The fix is a tightened check (every decline names a precondition that is actually unmet, verifiable against the recipe's own steps), scored on a later run.
  - The same run recorded two smaller artifact defects inside passing checks: the advisor's Step 4 invocation hard-codes one option while its prose says to premortem whichever option Step 3 picks, and `random-frameworks` used two concept-map nodes absent from its own declared inventory.
- **CLOSED 2026-09-10: `site/` is at zero known vulnerabilities.** The coordinated upgrade below was taken - `astro` 7.3.2, `@astrojs/starlight` 0.42.0, `sharp` 0.35.4, `astro-mermaid` 2.1.0 - and `npm audit` reports **0**. Verified beyond a green build: 269 pages, all 271 baseline routes, rendered-link guard clean, and an element-by-element A/B of four pages against the live Astro 6 site showing identical structure apart from one genuine rendering fix (the quickstart's pipe table, which Astro 6 MDX never parsed). The record of why it was scheduled rather than forced follows.
- ~~**OPEN, security: `site/` carries 6 advisories including a critical, and clearing them needs a major-version upgrade.**~~ History: #106 took it 10 -> 6 in August; by the v0.14.0 cut (2026-09-10) it had regressed to 10 against an *unchanged* `package-lock.json` (new advisories, same pinned versions); a non-breaking `npm audit fix` then took it back to 6 (1 low, 3 moderate, 1 high, 1 critical). **All six that remain need a major bump**, so there is no further non-breaking move:
  - `astro@7.3.2` - clears the **critical** XSS (GHSA-26w7-cxv4-gfx2, unescaped spread attribute names in `renderHTMLElement`; plus a second XSS via `transition:*` directive values) and a low `esbuild` dev-server file read on Windows.
  - `@astrojs/starlight@0.42.0` - clears three moderates (`@astrojs/mdx`, `@astrojs/starlight`, `astro-expressive-code`). Couples to the Astro major.
  - `sharp@0.35.4` - clears the high (inherited libvips CVE-2026-33327/33328/35590 and libheif GHSA-g89c-p67h-r497 / GHSA-2jg2-4ch7-h545).
  
  The work is one coordinated Astro 7 + Starlight 0.42 upgrade with a real regression surface: `site/` is a generated view of `skills/`, so a broken build is a broken product surface, and the route-parity baseline (271 routes) plus the rendered-link guard are the things that must stay green. Exposure meanwhile is build-time only - a developer machine and CI, not a plugin consumer - and the site is statically generated from repo-controlled content with no user input reaching a rendered attribute, which makes the XSS exposure low in practice. Low is a judgment, not a measurement, which is why this stays open.
- **Standard pin held at 0.8.** The Standard is at 0.13. At the pinned toolkit declaring `0.8` the repo returns advanced with 0 errors and 129 warnings (71 `G8` folder-README, 39 `G9` source-docblock, 7 `G7` docs-frontmatter, 5 `G10` Diataxis, 7 `U5` description-score); declaring `0.13` against the same toolkit returns convergent with 123 errors, which is the size of the debt. None of it is a defect this repo introduced; it is the Standard growing past a pin that has not moved. Rather than burn that debt now, the claim was scoped instead: README and `docs/conformance.md` state "advanced (Gold) ... **against Standard 0.8**" and say plainly that a newer toolkit reports more. Revisit at Standard 0.15, when the `S3` workflow mirror gates (see the release-process re-pin review).
- **Component `version` frozen at `0.1.0`.** Documented in `AUTHORING.md` step 7. The library-level version carries all semver meaning; a per-component bump ritual would be manual upkeep with no consumer.
- **All 67 sidecars covered by the lifecycle guard, including the four meta-skills.** The `META_SKILLS` exemption in `check-registry.mjs` is narrow by design: it exempts tools from "does this have a registry entry", not from lifecycle truth. The four tools read `status: active` with `maturity: alpha`, which is still honest after the 2026-09-10 skill-selection run: their **trigger** half is now measured, their **output** half is not, and `maturity: measured` requires both.

### Known open (raised 2026-08-14, not yet actioned)

- **The nine recipes in `_workflows/` are undeclared and uninvokable.** `library.json` has no `components.workflows` key and `manifest.generated.json` carries only skills and commands, so the README line "Recipes | 9 (skill chains shipped as workflow components)" has no runtime behind it. Toolkit ADR 0047 makes this a gating `S3` error at Standard 0.15. Fix for product reasons first.
- ~~**The front door is unmeasured.**~~ **CLOSED 2026-09-10** by the skill-selection eval - see the closed entry above. Raised as ADV-1 on 2026-07-11, absent from every roadmap until 2026-08-14, and open for two months after that because the blocker was a missing instrument that nobody had named. The **output** half followed the same day for three of the four: `framework-advisor`, `top3` and `random-frameworks` scored **20/20 checks** and now read `maturity: measured`. `think-research-framework` is the one still open, for a named instrument reason - see below.

## Resolved: runnable recipes, phase 1b (2026-08-15, #107)

**Was blocked, now shipped.** The nine recipe commands could not pass at the old pin: `S7` reported nine findings of the form `maps-to "think-audit-reasoning" but no skill or workflow by that name exists on disk` for files that were on disk. The pinned `command-contract.mjs` line 10 said `// ctx.workflows arrives in a later phase` and never built `ctx.workflows`, so no workflow-mapped command could resolve, and `maps-to` is mandatory so there was no way to write one that passed.

Resolved by moving the toolkit pin forward (PR #110) rather than by working around the check. The workarounds considered and rejected: dropping `maps-to`, pointing it at a skill, or suppressing `S7` would each have traded a true statement for a passing gate. The finding was a false negative in an old validator; the honest fix was to move the pin.

**Recorded because the sequence is the lesson:** the decision to hold the pin at Standard 0.8 was taken when its only visible cost was carrying warnings. Building phase 1b revealed a second cost that nobody could have predicted from the outside, and the decision was revisited on that evidence rather than defended. If a held pin ever blocks a feature again, re-measure before assuming the hold still nets out.

Footprint recorded (guardrail 6): +1,802 chars of always-loaded description across nine commands against the 33,233-char baseline, +5.42%, roughly 450 tokens per session. **Was still owed at the time:** the routing re-run. **Both halves are now paid (2026-09-10).** The framework-routing re-run reproduced (#119), and the interference question it could not answer was settled by the new skill-selection eval: across 794 cases the ten commands took the top pick twice (0.25%). Guardrail 6 now names the skill-selection instrument rather than the framework one.
## Open: six site/ vulnerabilities needing major-version bumps (2026-08-15, #106)

`npm audit fix` took `site/` from **10 vulnerabilities (7 high) to 6 (2 high)** without a breaking change, and the site still builds (268 pages). The remaining six all require **major** bumps and were deliberately not taken autonomously:

| Package | Severity | Fix requires |
|---|---|---|
| `astro` | high | `astro@7` (major) |
| `sharp` | high | `sharp@0.35` (major) |
| `@astrojs/mdx`, `@astrojs/starlight`, `astro-expressive-code` | moderate | `@astrojs/starlight@0.41.7` (major) |
| `esbuild` | low | `astro@7` (major) |

These are **build-time dependencies of the docs site**, not anything shipped to a plugin consumer, so the exposure is a developer machine and CI rather than a user. That is why the honest move is to schedule the Astro 7 / Starlight upgrade as its own piece of work with the site build verified page by page, rather than to run `npm audit fix --force` and discover the breakage in a 268-page rebuild.

**Do not run `--force` casually here.** The site is a generated view of `skills/`, so a broken build is a broken product surface, and the route-parity guard will (correctly) fail loudly if a major bump changes output paths.

## From the conformance-claim correction (2026-08-15)

- **Open: the tier claim is still hand-maintained, which is how it went stale for three minor versions.** The `0 errors / 0 warnings` claim was true at v0.10.0 and wrong from v0.11.0 onward, and nothing caught it because no check compares the published numbers against an actual evaluator run. CI already clones the pinned toolkit and runs `evaluate.mjs`, so the counts are available in the one place that could assert them. A guard that parses the evaluator summary and diffs it against the README claim would move this from "remember to update it" to "cannot be wrong", which is the C1 pattern (see the lifecycle guard). Not built yet; the manual fix is in place. **This is the highest-value remaining item in the truth category.**
- **Decided: keep the caveat-first descriptions and carry the seven `U5` warnings.** Rewording them to score better would launder exactly the weakness the contested-lens contract exists to surface. If a future Standard offers a way to declare the tension explicitly, take it upstream rather than letting either side quietly win.
- **Note for whoever grades this repo locally:** clone the CI-pinned ref into `.agent-skills-toolkit/` (gitignored, the same path CI uses) and run `npm ci` there. A sibling `../agent-skills-toolkit` checkout is almost certainly ahead of the pin and will report whole check families the pinned run does not.

## From the contested-guard hardening (2026-08-15, #103)

Shipped: preamble ratchet 6 to 3, soft-endorsement denylist, citation-shaped evidence token, HTML-table detection, and an advisory channel for the RT-1 residual. These were deliberately left.

- **RT-3b, the whitespace-aligned pseudo-matrix: a permanent structural residual, not a deferral.** A grid of the discredited artifact rendered with spaces rather than pipes or HTML is structurally indistinguishable from formatted prose, and any detector strict enough to catch it would false-positive on aligned lists and code-ish content. This is not waiting on effort or a better idea; it is the point where the structural layer correctly stops. It is named in the SCOPE NOTE of `scripts/lib/contested-lib.mjs` and carried as a `{ todo: true }` test so it stays visible, and it belongs to the mandated adversarial evidence-honesty pass. **Do not "fix" it with a stricter regex.**
- **RT-1's soft tail is advisory by design.** The blatant rehabilitation register is denied outright; the subtler "concede, then sell" shape raises a non-blocking warning instead of failing, because the honest phrasing ("the value here is the discipline this skill adds") sits genuinely close to the dishonest one. Promoting it to a hard failure would false-block real caveats. If the advisory ever fires on a shipped lens, that is a review prompt, not a bug.
- **The advisory channel is opt-in and currently has exactly one producer.** `checkContestedEntry` pushes into `opts.warnings` only when a caller supplies the array, so the signature change is additive. If a second advisory is added later, keep that property: callers that do not opt in must see identical behaviour.

## Pre-existing (predates this effort)

- **GA4 content-vs-acquisition decision.** Parked, waiting on the analytics signal (see `docs/internal/MEASUREMENT.md` and the content-plan). Tracked separately.
